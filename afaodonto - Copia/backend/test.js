const mysql=require('mysql2');const db=mysql.createPool({host:'localhost',user:'root',password:'',database:'AFA'}); db.query("INSERT INTO consultas (pacientes_id, servico_nome, servico_preco, custo_equipamento, data, status, observacoes) VALUES (?, ?, ?, ?, ?, 'pendente', ?)", [1, 'Teste', 100, 50, '2026-05-04 10:00:00', 'Teste'], (err, res) => { console.log(err ? err.message : 'OK'); process.exit(0); });
